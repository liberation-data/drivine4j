# Logging configuration for tests
logging:
  level:
    drivine: DEBUG
    sample: DEBUG
    org.neo4j.driver: INFO

# Spring configuration
spring:
  main:
    banner-mode: off
  output:
    ansi:
      enabled: always
  profiles:
    active: default  # Change from 'default' to 'local' to use local Neo4j instead of test containers

---
# Local profile configuration - for using local Neo4j instance
spring:
  config:
    activate:
      on-profile: local

# Data-source below is used then the 'local' config is active

database:
  dataSources:
    neo:
      type: NEO4J
      userName: neo4j
      password: h4ckM3$$$$
      host: localhost
      port: 7687
      databaseName: neo4j
