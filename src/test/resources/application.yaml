# Logging configuration for tests
logging:
  level:
    drivine: DEBUG
    sample: DEBUG
    org.neo4j.driver: INFO

# Spring configuration
spring:
  main:
    banner-mode: off
  output:
    ansi:
      enabled: always
  profiles:
    active: test

## Test configuration - set to true to use local Neo4j instead of Testcontainers
#test:
#  neo4j:
#    use-local: true  # Change to 'true' to use your local Neo4j instance

# Test datasource configuration
# When use-local=true: these settings are used as-is
# When use-local=false (default): host/port/password are overridden with Testcontainer values
database:
  datasources:
    neo:
      type: NEO4J
      host: localhost
      port: 7687
      user-name: neo4j
      password: brahmsian
      database-name: neo4j

---
# Local profile configuration - for using local Neo4j instance
spring:
  config:
    activate:
      on-profile: local

# Data-source below is used when the 'local' profile is active

database:
  datasources:
    neo:
      type: NEO4J
      user-name: neo4j
      password: brahmsian
      host: localhost
      port: 7687
      database-name: neo4j
