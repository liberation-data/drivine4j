spring:
  profiles:
    active: test
  main:
    banner-mode: off

logging:
  level:
    org.drivine: DEBUG

test:
  neo4j:
    use-local: true

database:
  datasources:
    graph:
      type: NEPTUNE
      host: drivine-test.cluster-cchissy84hz0.us-east-1.neptune.amazonaws.com
      port: 8182
      region: us-east-1
